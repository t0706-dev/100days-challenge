# 100日アプリチャレンジ

## プロジェクト概要
毎日1つアプリを作成する100日チャレンジ。
完成したアプリはXで進捗公開する。

## アプリリスト
`apps-list.md` を参照すること。

## 作成フロー（必ず守ること）
「Day〇を作って」と言われても、**すぐに作成してはいけない**。
以下のステップで要件を固めてから作成すること。

### Step 1：アプリ概要の提示
- apps-list.md から該当Dayのアプリ名・ジャンルを確認
- そのアプリの概要と、想定される基本機能を箇条書きで提示する

### Step 2：機能のヒアリング
以下の項目をユーザーに質問する（一度にまとめて聞く）

- 必須にしたい機能はあるか
- 外したい機能はあるか
- 追加したいこだわり機能はあるか

### Step 3：デザインのヒアリング
以下の項目をユーザーに質問する

- カラーイメージ（例：ダーク系・明るい・ポップ など）
- 雰囲気（例：シンプル・ゲームっぽい・かわいい など）
- その他こだわり（アニメーション・音など）

### Step 4：要件まとめ・確認
ヒアリング内容をもとに要件を整理して提示し、
「この内容で作成しますがよろしいですか？」と確認する。

### Step 5：作成
OKが出たら初めて作成を開始する。

---

## 出力ルール
- 出力先は `apps/dayXX-アプリ名/index.html`
- 単一HTMLファイルで完結させる（CSS・JSはインライン）
- スマホでも見やすいレスポンシブデザイン
- X投稿用にスクショ映えするUIにする
- フォントは M PLUS Rounded 1c（Google Fonts）を使用する

## フォルダ構成
```
/
├── CLAUDE.md
├── apps-list.md
├── .env
├── log_today.py
├── post_to_wp.py
├── logs/
├── tweets/
└── apps/
    ├── day01-dice/
    │   └── index.html
    ├── day02-coin/
    │   └── index.html
    └── ...
```

---

## 完成時のまとめ表示（必ず守ること）

ユーザーが「完成」「完成しました」「完成でお願いします」などと言ったら、
以下のフォーマットでまとめを表示すること。

### 表示内容

**① 記録コマンドの案内**

`log_today.py` は対話形式で個人の感想・学びを入力するスクリプトなので、ターミナルで直接実行してください。

```bash
cd /path/to/100days-challenge
python3 log_today.py
```

**② log_today.py で聞かれる内容の一覧（表形式）**

| 質問 | 入力例 |
|---|---|
| 今日は何日目？ | `{Day番号}` |
| 今日やったこと | `{アプリ名}を作成` など（空行で確定） |
| 学んだこと・気づき | `{実装で学んだこと}` など |
| 今日の感想を一言 | `{感想}` など |
| GitHub Pages URL | `https://t0706-dev.github.io/100days-challenge/apps/day{XX}-{英語名}/` |
| 絵文字の確認 | 提案される絵文字が合えば `y` |
| 明日のアプリ | apps-list.md から **{次のアプリ名}** が自動入力されます |

**③ 今日の完成内容（箇条書き）**

会話の中で作成・修正した機能を ✅ 付きで箇条書きにする。

**④ 締めの一言**

`お疲れさまでした 🎉` で締める。

---

## 今日の記録をつける

```bash
python3 log_today.py
```

このコマンドを実行すると、対話形式で今日の進捗を入力でき、
WordPressへの下書き投稿・ツイート文の生成・ログ保存が自動で行われる。
